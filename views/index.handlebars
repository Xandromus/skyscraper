<main>
    <div class="container middle">
        <div class="container">
            <div class="row mt-3 mx-auto articles-area">
                <div class="col-12">
                    <div class="px-0" id="articles">
                        {{#each articles}} {{> article-card }} {{/each}}
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>